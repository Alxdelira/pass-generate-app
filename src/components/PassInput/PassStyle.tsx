import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        backgroundColor: '#3ce59f',
        width: '100%',
        color: 'black',
        fontSize: 20,
        height: 50,
        borderRadius: 10,
        padding: 10,
        textAlign: 'center',
    },
});